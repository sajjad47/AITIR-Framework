# Pinned toolchain for repository validation and artifact generation.
# Install in an isolated environment with:
# uv run --with-requirements requirements-dev.txt <command>
cairosvg==2.9.0
jsonschema[format]==4.26.0
pillow==12.3.0
pymupdf==1.28.0
ruff==0.16.1
weasyprint==69.0
